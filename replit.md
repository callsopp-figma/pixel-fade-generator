# Pixel Fade Generator

## Running on Replit

This is a self-contained static web app. The Replit workflow serves the project root with Python's built-in HTTP server:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

Open the Replit preview to use the generator. No package installation, backend service, or environment secrets are required.