#!/usr/bin/env bash
set -euo pipefail

rm -rf dist
mkdir -p dist
cp index.html dist/index.html

[[ -f dist/index.html ]] || {
  echo "Expected build output at dist/index.html" >&2
  exit 1
}

figma make deploy --build-dir dist
