#!/usr/bin/env bash
set -euo pipefail

# Dependency-free static site: stage the served files into a clean output dir.
rm -rf dist
mkdir -p dist
cp index.html dist/

[[ -d "dist" ]] || {
  echo "Expected build output at dist" >&2
  exit 1
}
figma make deploy --build-dir "dist"
