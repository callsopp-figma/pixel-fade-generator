#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
[[ -f "$REPO_ROOT/index.html" ]] || { echo "Expected index.html at $REPO_ROOT" >&2; exit 1; }
figma make deploy --build-dir "$REPO_ROOT"
