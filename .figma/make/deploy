#!/usr/bin/env bash
set -euo pipefail

rm -rf dist
mkdir -p dist
cp index.html dist/index.html

[[ -d dist ]] || {
  echo "Expected build output at dist" >&2
  exit 1
}
figma make deploy --build-dir dist
