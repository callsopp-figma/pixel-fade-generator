#!/usr/bin/env bash
set -euo pipefail

# Dependency-free static site: the "build" is a copy of the served files.
BUILD_DIR="dist"
rm -rf "$BUILD_DIR"
mkdir -p "$BUILD_DIR"
cp index.html "$BUILD_DIR/"

exec figma make deploy-preview --build-dir "$BUILD_DIR"
