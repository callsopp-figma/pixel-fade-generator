#!/usr/bin/env bash
set -euo pipefail

# Dependency-free static site: "building" means staging the static assets.
BUILD_DIR="dist"
rm -rf "$BUILD_DIR"
mkdir -p "$BUILD_DIR"
cp index.html "$BUILD_DIR/"

exec figma make deploy-preview --build-dir "$BUILD_DIR"
